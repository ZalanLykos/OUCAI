<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="8dp"
    android:paddingBottom="4dp">

    <!-- Header with RECENT CHATS title and delete all button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="4dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="RECENT CHATS"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#888888"
            android:letterSpacing="0.1" />

        <ImageButton
            android:id="@+id/btn_delete_all_chats"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_trash_outline_24"
            android:contentDescription="Delete all conversations"
            android:padding="4dp"
            android:scaleType="centerInside" />

    </LinearLayout>

    <!-- RecyclerView for conversation items -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_recent_chats"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:overScrollMode="never"
        android:paddingBottom="8dp" />

</LinearLayout>
